import LoginScreen from './screens/LoginScreen';import React, { useState } from 'react';

export default LoginScreen;import { SafeAreaView } from 'react-native-safe-area-context';

import { View, Text, TextInput, TouchableOpacity, StyleSheet, ActivityIndicator } from 'react-native';
import { useRouter } from 'expo-router';